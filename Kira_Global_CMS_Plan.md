# マルチサイト統合CMS（Kira Global CMS）実装計画

## 概要
現在別のPCで稼働している「ごっちゃになってしまった4つのCMS」の機能をすべて吸収し、さらに「未開発だったこの旅行アプリ用のCMS」も新設する形で、**「全5サイト統合型の最強CMS」** をこのプロジェクト（`travel_app_v2`）内に新しく構築します。

UI/UXを大幅に改善し、「自分が今どのサイトを編集しているか」を一目でわかるようにすることで、誤操作（別のサイトへ投稿してしまう等の人為的ミス）を完全に防ぎます。

## Open Questions（確認事項）
この計画を進めるにあたり、以下の3点について確認が必要です。これによってデータベース接続の仕組みやUIの作り方が大きく変わります。

1. **この旅行アプリで管理したいコンテンツ**
   - この旅行アプリ（未開発のCMS）では、どのようなデータを管理できるようにしたいですか？
   - （例：「お知らせやニュースの配信」「新しい島やスポットのデータ追加」「ユーザーへのお知らせ機能」など）

2. **他の4つのサイトの種類と役割**
   - 他の4つのCMSは、それぞれどのようなサイトを管理するためのものでしょうか？（例：「会社のホームページ」「ブログ」「別サービスのアプリ」など、簡単な名前や役割を教えてください）

3. **他の4つのSupabaseプロジェクトのテーブル構造**
   - 他の4つのサイトのSupabase（データベース）には、「ブログ記事（articles）」など、それぞれ全く違うテーブル構造が入っていますか？
   - （※テーブル構造が全く違う場合、サイトを切り替えた時に「入力フォームの項目」も自動で切り替える仕組みを作ります！）

---

## Proposed Changes（提案する変更内容）

### 1. 接続管理モジュールの新設 (Backend)
各サイトごとのSupabaseプロジェクト（計5つ）に動的に接続するための仕組みを作ります。

#### [NEW] `src/lib/multiTenant.ts`
- 各サイトごとの環境変数（URL, Service Role Key）を管理。
- リクエストヘッダーから送られてくる `site_id` に応じて、適切なSupabaseクライアントを生成して返す `getAdminClient(siteId)` 関数を実装。

### 2. APIルートの改修 (Backend)
現在の管理用APIが、リクエスト元のサイトを識別できるようにします。

#### [MODIFY] `src/app/api/admin/*` の全API
- リクエストヘッダー `x-site-id` を受け取るように改修。
- `getAdminClient(siteId)` を使って、対象のSupabaseプロジェクトへクエリを投げるように変更。
- 新しく必要になるAPI（記事投稿API、画像アップロードAPIなど）を追加。

### 3. CMS管理画面のUI/UX大幅アップデート (Frontend)
誤操作を防ぐための絶対的な安全装置と、直感的なデザインを組み込み、本格的なCMSツールとしての機能を搭載します。

#### [MODIFY] `src/app/kira-system-panel-9f2/page.tsx`
- **サイトスイッチャーの導入**: 画面の目立つ場所に、5つのサイトを切り替えるドロップダウンを設置。
- **ダイナミック・テーマカラー**: 選択中のサイトに応じて、管理画面全体のテーマカラーを動的に変化させます。
  - 例：旅行アプリ＝🟦ブルー、ブログ＝🟩グリーン、企業サイト＝⬛️ブラックなど
- **サイト別ダッシュボード**: サイトごとに必要な管理機能（旅行アプリ用の島管理、ブログ用の記事投稿など）を出し分けるロジックを追加。
- **リッチコンテンツエディタ**: ブログ記事やお知らせを書けるように、太字や画像挿入ができるリッチテキストエディタを新設。

---

## Verification Plan（検証計画）

### 手動検証（Manual Verification）
1. 環境変数に「ダミーの第2サイト」用のSupabaseキーを一時的に設定。
2. CMS画面で「旅行アプリ」と「第2サイト」を切り替え、テーマカラーや入力フォームが瞬時に変わることを確認。
3. 記事一覧を取得した際、それぞれ「別々のSupabaseプロジェクト」から正しくデータが取得できているかを確認。
